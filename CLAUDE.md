# Phronesis Product Site — Operating Pointer (CLAUDE.md)

**Scope.** The `phronesis-product-site` repo — the public marketing/discovery surface at `phronesisintel.com` (Cloudflare Pages, auto-deploys `main` on merge). This is the **Phronesis** workstream; never the SFP Trading System.

## §0. Roles — canonical pointer

**Governance-lane roles & responsibilities are canonical in `Agent_Org_Roles_and_Responsibilities`** (workspace `00_Operating_Model/`; app-repo mirror `docs/governance/00_Operating_Model/`; **referenced by name, never version** — Q-AGENT-RR, Ledger §15.10.256/.257). In brief, as applied here: COO authors all public copy (Logos voice-gate + owner approval); CCO wires/renders it verbatim and honesty-gates claims against code; the owner merges and the merge is the deploy (two-actors).

## §1. Binding gates on this repo (CI-enforced)

- **Algorithm-protection / banned-language gate** — no model-vendor or model-tier names on marketing surfaces (legal substrate files are allow-listed for processor disclosure: `privacy.html`, `privacy-policy.html`, `terms-of-service.html`, `refund-policy.html`; crawler directives in `robots.txt`).
- **No-internal-taxonomy-leaks gate** — internal ring/component names never appear on public surfaces.
- Canonical-reference, vertical-count-parity, stale-reference, and Trust-Center-link gates.

## §2. Discipline

Copy changes trace to a Logos-gated source document in the workspace `02_Marketing/` (render verbatim; trims only via the honesty gate, flagged in the PR). The canon spine lives in `Phronesis_Canon_Surface_Recut_v1` and successors; surface-vs-canon drift is a Surface Ops Steward integrity duty.

*v1 — 2026-06-11, created as part of the Q-AGENT-RR reference wiring (owner merge = approval).*
